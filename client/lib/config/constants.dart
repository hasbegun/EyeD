const int maxDashboardResults = 50;
const int maxHistoryEntries = 500;
const Duration healthPollInterval = Duration(seconds: 5);
const int wsReconnectBaseMs = 1000;
const int wsReconnectMaxMs = 30000;
const int wsReconnectFactor = 2;
